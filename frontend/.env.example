# Worker API URL (required when frontend and API are on different domains).
# Local dev uses the Vite proxy in vite.config.ts — leave unset for `npm run dev`.
VITE_API_URL=https://shelf-analysis-api.steven-walsh39.workers.dev
